export type NavLink = {
  label: string;
  href: string;
};

export type Feature = {
  eyebrow: string;
  title: string;
  description: string;
};

export type StoreInfo = {
  name: string;
  address: string;
  mapUrl: string;
  hours: string;
  locationLine: string;
};
